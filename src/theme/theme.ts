import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "rgb(8, 126, 164)",
    },
  },
  typography: {
    fontFamily: "roboto",
  },
});

export default theme;
